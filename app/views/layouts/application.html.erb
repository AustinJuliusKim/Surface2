<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Surface2</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container-fluid wooden">
	<nav>
	<% if logged_in? %>
		<p>Logged in as: <%= current_user.username  %> | <%= link_to 'Log Out', logout_path, method: :delete  %></p>
	<% else %>
		<div>
			<%= link_to 'Log In', login_path  %> | <%= link_to 'Sign Up', new_user_path  %>
		</div>
	<% end %>
	</nav>

	<% if flash[:welcome] %>
	<h4 class="bg-success flash">
		<%= flash[:welcome]  %></h4>
	<% end %>

	<% if flash[:error] %>
	<h4 class="bg-danger flash">
		<%= flash[:error]  %></h4>
	<% end %>
<%= yield %>
</div>
</body>
</html>
